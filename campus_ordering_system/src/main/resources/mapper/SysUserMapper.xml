<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.ordering.mapper.SysUserMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.campus.ordering.entity.SysUser">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="gender" property="gender"/>
        <result column="user_status" property="userStatus"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询列 -->
    <sql id="Base_Column_List">
        user_id, username, password, nickname, avatar, phone, email, gender,
        user_status, is_deleted, create_time, update_time
    </sql>

    <!-- 根据用户名查询用户信息 -->
    <select id="selectByUsername" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM sys_user 
        WHERE username = #{username} 
        AND is_deleted = 0
    </select>
</mapper>
