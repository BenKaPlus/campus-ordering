<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.ordering.mapper.ShoppingCartMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.campus.ordering.entity.ShoppingCart">
        <id column="cart_id" property="cartId"/>
        <result column="user_id" property="userId"/>
        <result column="shop_id" property="shopId"/>
        <result column="product_id" property="productId"/>
        <result column="spec_id" property="specId"/>
        <result column="product_num" property="productNum"/>
        <result column="is_checked" property="isChecked"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询列 -->
    <sql id="Base_Column_List">
        cart_id, user_id, shop_id, product_id, spec_id, product_num, is_checked,
        is_deleted, create_time, update_time
    </sql>

    <!-- 查询用户购物车列表 -->
    <select id="selectByUserId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM shopping_cart 
        WHERE user_id = #{userId} 
        AND is_deleted = 0
        ORDER BY create_time DESC
    </select>
</mapper>
